import React from 'react'
import styled from 'styled-components'

const Header = styled.header`
  color: white;
  margin: 30px 0;
  text-align: center;
`

export default () => (
  <Header>
    <h1>Lisk Transactions</h1>
  </Header>
)
